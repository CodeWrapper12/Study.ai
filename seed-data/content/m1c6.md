## Exercise: House Robber

For each list below (non-adjacent house values), print the maximum loot. One line per case, in order.

```python
cases = [
    [1, 2, 3, 1],
    [2, 7, 9, 3, 1],
    [5],
]
```

**What's being trained:** the five-step DP procedure compressed to two variables — state, recurrence, base, order, squeeze. If you wrote a dp array first, good; now do the O(1)-space version.
