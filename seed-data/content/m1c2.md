## Exercise: Two Sum (print the indices)

For each `(nums, target)` pair below, print the two indices (smaller first, space-separated) of the numbers that add to the target. Exactly one solution exists per case. One line per case, in order.

```python
cases = [
    ([2, 7, 11, 15], 9),
    ([3, 2, 4], 6),
    ([3, 3], 6),
]
```

**What's being trained:** the seen-map pattern with store-after-check — case 3 exists specifically to catch the duplicate-handling order. O(n) per case required.
