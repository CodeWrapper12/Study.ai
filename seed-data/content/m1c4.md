## Exercise: Lower Bound

Implement `lower_bound(nums, target)` returning the index of the **first element ≥ target** (or `len(nums)` if none). Then, for the fixed sorted list and each query below, print the result. One line per query, in order.

```python
nums = [1, 3, 3, 5, 8, 13]
queries = [3, 4, 0, 14]
```

**What's being trained:** the half-open `[lo, hi)` template from Lesson 4 — exactly the convention, no improvising. Queries cover: existing duplicate, gap, before-all, after-all.
