## Exercise: Longest Unique Substring Length

For each string below, print the length of its longest substring without repeating characters. One line per string, in order.

```python
strings = ["abcabcbb", "bbbbb", "pwwkew", ""]
```

**What's being trained:** the variable window derived from its invariant — expand right greedily, shrink left until "no repeats" is restored. The empty string is in the list on purpose.
